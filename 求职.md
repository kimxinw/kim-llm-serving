# kim-llm-serving 秋招求职版收尾设计

## 1. 文档定位

本文用于指导 `kim-llm-serving` 在秋招投递前完成最后一次架构闭环、实验验证和求职交付。

本轮不追求覆盖完整生产级 LLM Serving 平台，而是交付一个边界清晰、真实可运行、故障可收敛、资源有上限、性能可复现，并包含一项可量化系统创新的单机推理服务作品。

目标版本定义为：

> **v0.1：单 GPU、单模型、独立 C++ Worker、OpenAI-compatible SSE、端到端有界流控、生命周期可靠，并通过分层过载治理验证 SLO goodput。**

目标岗位按优先级为：

| 优先级 | 岗位方向 | 项目主要证据 |
|---:|---|---|
| P0 | LLM Inference / Serving Runtime Engineer | C++ Runtime、TensorRT-LLM、流式生命周期、背压与资源治理 |
| P0 | 大模型推理系统工程师 | Executor 接入、请求状态机、Admission、Benchmark |
| P0 | AI Infra 推理方向 / Model Serving Data Plane | 进程隔离、IPC、故障传播、可观测与 SLO |
| P1 | MaaS / 模型服务平台研发工程师 | OpenAI API、健康检查、过载拒绝、服务边界 |
| P1 | 高性能 C++ / 机器学习系统工程师 | C++17、并发、RAII、有限状态机与性能实验 |

---

## 2. 当前已验证基线

截至 2026-08-27，仓库实际状态如下。本文后续所有“已完成”均以代码、测试或提交证据为准。

| 模块 | 当前状态 | 证据或边界 |
|---|---|---|
| TensorRT-LLM 0.16 / TensorRT 10.7 隔离环境 | 已完成 | 固定 CXX11 ABI 0，RTX 3060 单卡环境 |
| TinyLlama 1.1B FP16 Engine | 已完成 | 已有 Token ID 真实 Engine 基线 |
| `GenerationBackend` / Executor 接入 | 已完成 | `TrtLlmExecutorBackend` |
| `TokenDelta` / 唯一 `TerminalEvent` | 已完成 | Generation Types 与生命周期测试 |
| 有界 `GenerationMailbox` | 已完成 | Token 容量有界，Terminal 具有保留语义 |
| `RequestState` | 已完成 | 取消、超时、背压与终态门禁 |
| `AdmissionController` / `AdmissionLease` | 已完成 | 活动请求、输入 Token、预留输出 Token 三类原子预算 |
| `GenerationRuntime` | 已完成 | commit `82a750b`；Admission 已接入提交、终态和停止链路 |
| IPC v1 完整消息与帧 codec | 已完成 | commits `91c79ae`、`9e5196d`；覆盖握手及 Submit/Accepted/Rejected/Cancel/TokenDelta/Terminal/Health/Stats，具备严格字段、版本、epoch 与长度校验 |
| CPU / 无 GPU 契约测试 | 已完成当前基线 | 新增 SLO Policy 契约后共 16 项；2026-08-27 当前构建完整回归 `16/16 PASS`（`6.03 s`） |
| Direct / IPC Benchmark | A6-5 固定 workload 基线完成 | commit `3334046`；完成同 workload closed-loop、3轮交错配对、逐请求 Token 一致性和增量统计 |
| UDS Transport / IPC Session | A6-2 已完成并推送 | commit `b625df0`；已有真实 UDS Listener/Connection、握手型 IpcSession、有界单写者、半帧/断连和资源回收契约 |
| Runtime Bridge / SessionEgress | A6-3 已完成并推送 | commit `050a263`；完成请求归属、Submit/Cancel 转换、Accepted 先行、EventForwarder、终态感知公平 Egress、请求级背压与断连取消；Fake Backend + 真实 UDS CPU 契约通过 |
| 推理请求完整流程文档 | 已完成当前版本 | commit `5b3922b`；`docs/Inference_Pipeline.md` 记录 Submit 到 TensorRT-LLM、Mailbox、Egress 和 Terminal 返回的完整代码路径 |
| 模块化源码与 CPU-only 构建 | 已完成并推送 | commit `392ae93`；源码/测试按职责镜像分类，测试链接实际模块库；最新提交在 TRT-LLM OFF 空目录构建中通过 10 项无 GPU 契约 |
| `WorkerServer` / 独立 `llm_worker` | 已完成基础版 | commit `1676515`；完成单活 Session 管理、独立进程入口、严格配置加载、Runtime 组合与优雅停止 |
| Python `GenerationClient` | 已完成基础版 | commit `1676515`；覆盖握手、请求多路复用、Rejected、Worker 断连合成 `Unavailable`、客户端有界缓冲与 Manifest 校验 |
| 真实 Worker / Engine 跨进程链路 | A6-4 已完成并推送 | commits `a2f9c9b`、`2589c4e`；Python Client 经 UDS 调用真实 `llm_worker` 和 TinyLlama Engine，流式/非流式 Token IDs 均与 Direct 基线完全一致；真实 Worker `SIGKILL` 后两个已 Accepted 请求分别收敛为唯一 `Unavailable` Terminal |
| A6-5 Engine Evidence 阶段门禁 | 已完成并推送 | commit `3334046`；C1/ISL4/OSL32、每路径每轮200请求、3轮交错配对；Token IDs 全量一致，失败/Rejected/Backpressure/Cancel 均为0，资源归零；IPC 增加 TTFT P50 `0.661 ms`、E2E P50 `1.482 ms`，吞吐下降 `0.66%` |
| Gateway / Tokenizer / HTTP / SSE | 最小版已完成并推送 | commit `0581c0a`；固定模型 Tokenizer/Chat Template/Manifest 校验、OpenAI-compatible 非流式与 SSE、health/readiness/metrics、请求级有界输出、HTTP 断连 Cancel 与 Terminal 排空均已实现 |
| HTTP/SSE Benchmark Harness | 代码、CPU 契约与正式分层基线已完成 | commits `c37cc81`、`c3f24ab`、`b192344`、`e376f78`；支持隔离 Worker/Gateway、closed/open-loop、有界 `max_inflight`、固定/Poisson 到达、SLO goodput、拒绝分类、慢客户端分组、资源归零与证据门禁；C1/ISL17/OSL32 Direct/IPC/HTTP 三轮轮换基线已落盘 |
| 慢客户端隔离正式实验 | 已完成并推送 | commit `2b08de1` 固化干净提交 `e376f78` 上的8 RPS open-loop、ISL17/OSL32三轮control/slow交替配对；健康请求339/339满足TTFT/E2E SLO，Slow相对Control的健康TTFT P95增加`0.572 ms`、E2E P95增加`3.804 ms`，全部请求、Terminal和资源收敛 |
| CPU-only configure / CI / 一键入口 | 部分完成 | CPU-only configure/build/test 已完成；CI 与一键入口未完成 |
| 分层过载治理正式实验 | 已完成 | 干净提交 `969897b`；16/24/32 RPS、3策略×3轮、共27组运行。Token Budget 减少 Accepted 后 SLO miss；Profile-guided 因画像过于保守明显降低 goodput，不声明正收益 |

当前准确定位是：

> 项目已经完成 Worker-safe Generation Core、A6-1～A6-5 IPC 数据面、Engine Evidence 门禁、最小 OpenAI-compatible Gateway、HTTP/SSE Benchmark Harness、三路径分层基线、慢客户端隔离和三策略正式过载矩阵。矩阵中 Token Budget 在中高负载下将 E2E P95 从 Fixed 的 `425.444/426.366 ms` 降至 `304.864/314.173 ms`，但 goodput 配对差异的方差较大，不声明稳定提升；Profile-guided 因离线画像平均高估 TTFT P95 约 `16.2 ms` 而过度早拒绝，goodput 明显低于 Token Budget。当前进入 CPU CI、一键入口、文档和面试材料冻结，不再为获得正结果进行事后调参。

---

## 3. 设计目标与非目标

### 3.1 必须满足的目标

1. **完整链路**：OpenAI Client 能经 Gateway、UDS、C++ Worker 和 TensorRT-LLM 完成流式及非流式生成。
2. **生命周期可靠**：Accepted 请求在连接存活时最终恰好一个 Terminal；取消、超时、背压、断连和停止均能收敛。
3. **全链路有界**：请求数、Token、Mailbox、IPC 帧、Worker egress 和 SSE 输出均有显式容量。
4. **进程与 ABI 隔离**：Legacy TensorRT-LLM、TensorRT 和 ABI 0 只存在于 `llm_worker`。
5. **可验证创新**：分层过载治理必须通过固定 workload 和对照实验评价，不以主观描述代替数据。
6. **可复现交付**：CPU 路径可独立构建测试；GPU 实验关联 commit、Engine、环境与 workload。

### 3.2 本轮明确不做

- 自研 Paged KV Cache、Continuous Batching 或 Attention/GEMM Kernel；
- 多 GPU、Tensor Parallel、Prefill/Decode 分离；
- 多 Worker 路由、自动拉起、跨机器通信和复杂重连；
- 多模型动态加载、LoRA 多租户；
- Tool Calling、Logprobs、鉴权、TLS 和完整 OpenAI 参数集合；
- Kubernetes、Autoscaling 和集群级调度；
- 在线机器学习式 SLO 预测器。

这些能力超出单周收尾范围，也会削弱项目最有价值的“请求生命周期与资源边界”主线。

---

## 4. 求职版最终架构

```text
OpenAI Client
      │ HTTP / SSE
      ▼
Python Gateway
      ├── OpenAI Request Adapter
      ├── Tokenizer / Chat Template
      ├── ModelManifest Validator
      ├── Per-request SSE Queue + Disconnect Cancel
      ├── Gateway Metrics
      └── GenerationClient
              │
              ▼
Versioned UDS IPC v1
      ├── Hello / HelloAck / Health / Stats
      ├── Submit / Accepted / Rejected / Cancel
      ├── TokenDelta(batch) / Terminal
      ├── protocol_version / worker_epoch / request_id
      └── bounded frame + bounded session egress
              │
              ▼
C++ llm_worker
      ├── WorkerServer
      ├── IpcSession
      │     ├── Frame Reader / Dispatcher
      │     ├── Request Ownership Registry
      │     └── Terminal-aware Fair Writer
      ├── EventForwarder
      ├── Worker Metrics / Model Stats
      └── GenerationRuntime
              ├── Profile-guided SLO Policy（软门禁，可关闭）
              ├── AdmissionController（硬容量门禁）
              ├── AdmissionLease
              ├── RequestState
              ├── GenerationMailbox
              └── GenerationBackend
                      │
                      ▼
              TrtLlmExecutorBackend
                      │
                      ▼
              TensorRT-LLM Executor → RTX 3060
```

### 4.1 为什么使用进程隔离

TensorRT-LLM 0.16、TensorRT 10.7 和 CXX11 ABI 0 与现代 Python Gateway、其他 Tensor Runtime 或默认 ABI 依赖不适合进入同一地址空间。独立 Worker 的代价是一层序列化和进程运维，但换来：

- ABI 与依赖冲突被限制在 Worker；
- Worker 崩溃可以被识别为服务不可用，而不是拖垮 Gateway；
- Gateway 协议、Tokenizer 与 HTTP 生态可以独立演进；
- 后续替换 TensorRT-LLM 版本或 Backend 时不改变外部 API。

因此本轮接受 IPC 开销，并用 Direct/IPC/HTTP 分层 Benchmark 测量它，而不是凭感觉提前优化。

### 4.2 为什么显式增加 `IpcSession`

UDS 不是 `GenerationRuntime` 的职责。`IpcSession` 单独负责帧解析、握手、多路复用、连接级请求归属和有界写出，可以防止网络生命周期污染生成状态机。

`GenerationRuntime` 只接受结构化 Generation 请求并产生内部事件，不感知 Socket、JSON、HTTP 或 SSE。这保证 Runtime 可以继续由 Fake Backend 进行 CPU 测试。

### 4.3 为什么使用 Python Gateway

Python Gateway 可以直接复用 Hugging Face Tokenizer、Chat Template、FastAPI 和 OpenAI 客户端生态，缩短服务闭环时间。代价是双语言调试和 IPC 维护，但核心并发、资源治理和推理生命周期仍在 C++ Worker，不会把项目退化为 Python API 包装。

### 4.4 与主流开源推理框架的对标和取舍

本项目不宣称复刻 ChatGPT、Claude 等未公开的内部数据面；求职材料只引用可核验的开源实现，并明确“采用的架构原则”和“本项目自己的实现”之间的边界。

| 参考项目 | 可核验的公开设计 | 本项目采用 | 本项目调整或不采用 |
|---|---|---|---|
| [vLLM V1 Architecture](https://docs.vllm.ai/en/latest/design/arch_overview/) | API Server、EngineCore、GPU Worker 多进程分层；API Server 通过 ZMQ 与 EngineCore 通信 | API/Tokenizer 与 GPU Runtime 分进程；请求异步多路复用；故障域隔离 | v0.1 不引入多 EngineCore/多 GPU；已有帧协议继续使用原生 UDS，不切换 ZMQ |
| [vLLM EngineCore Client](https://github.com/vllm-project/vllm/blob/main/vllm/v1/engine/core_client.py) | 单独的 Engine Client 管理输入、输出、序列化和进程生命周期 | `GenerationClient` 与 Worker IPC 独立于 HTTP；连接断开统一收敛活动请求 | 不复制其 Python 对象和 MsgPack 结构；使用本项目稳定的 Token 事件契约 |
| [SGLang TokenizerManager](https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/managers/tokenizer_manager.py) / [DetokenizerManager](https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/managers/detokenizer_manager.py) | Tokenizer、Scheduler、Detokenizer 通过异步消息通道分离；按 request id 聚合增量结果 | Gateway 负责 Tokenizer/增量 Detokenizer；Worker 只传 Token IDs；请求状态按 id 隔离 | v0.1 不拆独立 Detokenizer 进程；固定单模型，减少进程和运维复杂度 |

对标结论固定为：主流框架证明了“外部 API/Tokenizer 与 Engine Core 分进程、通过异步 IPC 交换请求和增量结果”是成熟方向；原生 UDS、严格 JSON、Terminal 保留和分层 Admission 是本项目结合 C++17、Legacy ABI 与单机求职作品范围作出的具体取舍。后续简历使用“参考/对标”，不得写成“与某闭源系统内部实现相同”。

---

## 5. 组件职责与禁止依赖

| 组件 | 负责 | 不负责 |
|---|---|---|
| Gateway | HTTP/SSE、Tokenizer、Chat Template、客户端取消、外部错误映射 | GPU 调度、请求资源预算、TensorRT-LLM 类型 |
| `GenerationClient` | IPC 连接、握手、多路复用、Worker 断连收敛 | 模型执行和 Admission 决策 |
| `WorkerServer/IpcSession` | Socket 生命周期、协议校验、请求归属、有界 egress | Tokenizer、HTTP、GPU Scheduler |
| `EventForwarder` | Mailbox 到 IPC 的非阻塞桥接、公平写出 | Response Pump、用户回调 |
| `GenerationRuntime` | start/submit/cancel/stop 编排、策略和硬 Admission 顺序 | Socket I/O、JSON、SSE |
| SLO Policy | 基于画像和负载快照进行软性早拒绝 | 修改硬容量、保证预测绝对准确 |
| `AdmissionController` | 原子硬容量约束和 Lease | 性能预测、排队调度 |
| `GenerationMailbox` | 每请求有界事件交付和 Terminal 保留 | 跨请求网络公平性 |
| Backend | TensorRT-LLM Executor 生命周期与响应转换 | 外部协议和服务端连接 |

依赖方向固定为：外层可以依赖内层公共接口，内层不得反向依赖 Gateway 或 IPC 类型。

### 5.1 v0.1 会话与并发模型

v0.1 只允许一个活跃 Gateway Session；第二个连接必须明确拒绝，不实现多 Session 负载均衡、抢占或自动重连。接口可以保留扩展点，但本轮测试和简历均不得宣称多 Gateway 能力。

| 执行单元 | v0.1 数量 | 职责 | 有界条件 |
|---|---:|---|---|
| Worker accept thread | 1 | 接收唯一 Gateway 连接和管理 Session 生命周期 | 最多一个活跃 Session |
| Session reader | 1 / Session | Socket 读取、帧解码、协议校验和命令分发 | 帧长度、输入 Token 和请求数均有限制 |
| Session writer | 1 / Session | Socket 的唯一写入者，执行控制优先与请求级公平轮转 | egress frame/byte 高水位和 Terminal 保留容量 |
| `EventForwarder` RequestPump | 1 / Accepted 请求 | 阻塞消费该请求 Mailbox，并投递 IPC 事件 | 线程数受 Admission `max_active_requests` 硬限制，v0.1 上限不超过 8 |

请求级 Pump 是求职版的显式范围取舍：利用已有阻塞 Mailbox 获得清晰的取消和终态语义，线程数量由 Admission 硬限制，不形成无界 thread-per-request。若正式数据证明线程调度成为瓶颈，再替换为共享 Ready Queue/eventfd 驱动的统一 EventForwarder；本轮不预先实现复杂 Reactor。

---

## 6. 关键数据流与故障语义

### 6.1 启动与就绪

```text
worker start
  → Backend start
  → Engine / ModelManifest 校验
  → Runtime start
  → Admission open
  → UDS accept

gateway connect
  → Hello(protocol, expected model)
  → HelloAck(protocol, worker_epoch, model manifest, limits)
  → manifest 一致且 Runtime ready
  → /readyz = 200
```

`/healthz` 只表示 Gateway 进程存活；`/readyz` 必须同时满足 IPC 已握手、Worker epoch 有效、模型一致、Runtime running 且 Admission open。两者不得混用。

### 6.2 提交

```text
HTTP request
  → 参数、Token 和长度校验
  → Submit IPC
  → Runtime validation
  → SLO Policy（可关闭的软门禁）
  → Admission tryAcquire（不可绕过的硬门禁）
  → Lease 移入 Mailbox
  → Backend submit
  → 成功后返回 Accepted
```

`Accepted` 只能在 Runtime 已接收请求、资源已预留且 Backend submit 成功后发送。`Rejected` 表示请求从未进入活动生命周期，之后不得再发送 Terminal。

### 6.3 Token 与终态

```text
Executor response
  → Backend Response Pump
  → RequestState
  → GenerationMailbox
  → EventForwarder
  → IpcSession fair writer
  → GenerationClient
  → per-request SSE queue
  → HTTP client
```

任何网络或 HTTP I/O 都不得运行在 Backend Response Pump 上。

Terminal 成功提交到 Mailbox 后立即释放 AdmissionLease，而不是等待 IPC 或客户端消费。原因是 GPU/Runtime 资源生命周期在生成完成时已经结束，不能被慢网络客户端长期占用。网络层继续负责交付最终结果，但不再持有 GPU Admission 预算。

### 6.4 取消、断连与停止

| 事件 | 处理语义 |
|---|---|
| HTTP客户端断连 | Gateway 发送 Cancel；继续消费该请求 IPC 事件直到 Terminal 或 Worker 断连 |
| SSE单请求队列满 | 只取消该请求，不阻塞同一 IPC 会话的其他请求 |
| Gateway IPC断连 | Worker 取消该 session 拥有的所有活动请求 |
| Worker断连或epoch变化 | Gateway 对所有已 Accepted 未终态请求各合成一个 `Unavailable` |
| Cancel重复或请求已完成 | 幂等，不生成第二个 Terminal |
| graceful stop | Admission close → 拒绝新请求 → 收敛活动请求 → Backend stop → 关闭 IPC |
| 强制超时退出 | 未完成请求在 Gateway 侧收敛为 `Unavailable`，不得永久悬挂 |

---

## 7. 端到端不变量

| 不变量 | 设计理由 |
|---|---|
| Accepted 请求在传输存活时恰好一个 Terminal | 防止调用方永久等待或重复结算 |
| Terminal 后不得再出现 TokenDelta | 保证流式状态单调收敛 |
| Rejected 请求不产生 Terminal | 区分“未进入生命周期”和“已接受后完成” |
| Terminal 提交成功后才从 Runtime 状态表清理 | 防止终态丢失后请求状态提前消失 |
| AdmissionLease 恰好释放一次 | 保证资源守恒，避免容量永久泄漏或重复归还 |
| 所有队列、帧和 Token 都有上限 | 防止慢客户端或突发流量形成无界内存 |
| 控制消息和 Terminal 具有保留容量 | 普通 Token 不能挤掉取消与终态 |
| 单个慢请求不能阻塞其他请求 | 避免共享 FIFO 队头阻塞扩大故障面 |
| request ID 与 worker_epoch 共同标识生命周期 | Worker 重启后拒绝旧响应污染新请求 |
| Gateway 与 Worker 校验同一 ModelManifest | 防止 Tokenizer、特殊 Token 与 Engine 不一致 |

这些不变量优先于吞吐优化；任何性能优化若破坏其中一项，均不得合入求职版。

---

## 8. ModelManifest 与协议一致性

Gateway 与 Worker 各自持有模型的一部分，不能只依赖人工约定。v0.1 使用固定、版本化的 `ModelManifest`：

| 字段 | 用途 |
|---|---|
| `model_id` / `revision` | 对外模型身份与版本 |
| `tokenizer_fingerprint` | 校验 Tokenizer 文件或版本 |
| `chat_template_fingerprint` | 防止模板变更导致 Token 不一致 |
| `engine_fingerprint` | 关联 Engine 配置与实验结果 |
| `eos_token_id` / `pad_token_id` | 校验特殊 Token |
| `max_input_tokens` / `max_output_tokens` / `max_sequence_tokens` | 统一请求边界 |
| `precision` / `max_batch_size` | readiness 与 Benchmark 元数据 |

`HelloAck` 返回 Worker manifest 摘要，Gateway 在一致后才进入 ready。v0.1 不实现动态模型发现，因为固定单模型更容易保证实验可复现和故障语义明确。

---

## 9. IPC、队列与背压设计

### 9.1 IPC v1

第一版继续使用 `UDS + 4-byte 网络序长度前缀 + 严格 JSON`。

| 选择 | 原因 | 代价与缓解 |
|---|---|---|
| UDS 而非 TCP/gRPC | 单机低运维成本、权限边界清晰、无需暴露网络端口 | 不支持跨机；本轮明确非目标 |
| JSON 而非 Protobuf | 易调试、现有 codec 已完成、消息体较小 | 序列化开销更高；TokenDelta 批量化并用 Benchmark 决定是否升级 |
| 显式长度前缀 | 正确处理拆包、粘包和非法长度 | 需要状态化 decoder；已有 CPU 契约覆盖 |
| 严格字段与版本校验 | 尽早暴露协议漂移 | 演进较保守；后续再增加 capabilities |
| 单 Gateway 会话 | 缩小 v0.1 实现范围 | 会话断开影响全部请求；用 epoch、请求归属和故障收敛控制 blast radius |

最小消息集：

| 消息 | 方向 | 终态规则 |
|---|---|---|
| `Hello/HelloAck` | 双向 | 建立版本、epoch、manifest 与容量边界 |
| `Submit` | Gateway → Worker | 必须得到 Accepted 或 Rejected |
| `Accepted/Rejected` | Worker → Gateway | Accepted 才创建外部活动生命周期 |
| `TokenDelta` | Worker → Gateway | 支持多个 Token 的批量帧与 sequence number |
| `Terminal` | Worker → Gateway | 每个 Accepted 请求最多一个 |
| `Cancel` | Gateway → Worker | 幂等，不能阻塞结果写出 |
| `Health/Stats` | Health：Gateway → Worker；Stats：Worker → Gateway | 不进入生成热路径 |

除 `Hello` 外，所有请求生命周期消息必须携带 `protocol_version`、`worker_epoch` 和 `request_id`；`HelloAck` 返回非零 epoch 与 ModelManifest 摘要。跨进程 deadline 只传剩余 `timeout_ms`，禁止传 `steady_clock::time_point`。状态码、FinishReason 和消息类型使用稳定字符串，不直接序列化 C++ enum 数值或对象内存布局。

`Accepted` 必须在 Runtime 成功预留资源且 Backend submit 成功后发送，并在同一请求的任何 `TokenDelta/Terminal` 之前进入 egress；`Rejected` 表示请求从未进入活动生命周期，之后不得产生 Terminal。协议 v1 在 `Submit/Accepted/Rejected/Cancel/TokenDelta/Terminal/Health/Stats` 与 ModelManifest 字段全部确定前不宣布冻结。

### 9.2 分层有界流控

| 层级 | 容量 | 饱和处理 |
|---|---|---|
| Runtime Admission | active/input/reserved-output Token | 接收前明确 Rejected |
| 每请求 Mailbox | Token 事件 + Terminal 保留槽 | 终结该请求为 Backpressure |
| Worker session egress | 全局上限 + 控制/终态保留 | 公平调度；数据拥塞不得挤掉控制消息 |
| Gateway per-request queue | 单请求 SSE 输出上限 | Cancel 该请求并继续排空至 Terminal |
| IPC frame / HTTP body | 字节与 Token 上限 | InvalidInput，拒绝进入 Runtime |

Worker writer 使用控制/终态优先和请求级公平轮转，避免一个长输出请求独占连接。第一版不追求复杂 WFQ；简单 round-robin 足以验证隔离效果。

### 9.3 Terminal-aware Fair Egress

Session egress 不使用单个无界 FIFO，而使用控制队列和请求级队列：

```text
SessionEgress
  ├── control: HelloAck / Accepted / Rejected / Health / Stats
  └── requests:
        request A: TokenDelta → TokenDelta → Terminal
        request B: TokenDelta → Terminal
        request C: TokenDelta → TokenDelta → TokenDelta → Terminal
```

调度规则：

1. 控制消息使用独立保留容量，但不得让同一请求的 Terminal 越过已成功入队的 Delta。
2. 请求队列之间采用 round-robin；同一请求内部严格 FIFO，保证 sequence 与 Terminal 顺序。
3. 每个 Accepted 请求在 egress 中预留一个 Terminal frame credit；普通 Delta 不能消耗该容量。
4. 单请求队列饱和时只取消该请求，并继续排空至 Terminal；不得阻塞或取消其他请求。
5. Session 总字节或 Socket 失败导致控制/Terminal 也无法交付时，关闭连接；Gateway 对所有 Accepted 未终态请求合成 `Unavailable`。

必须分别记录 per-request queue 和 session egress 的当前值、高水位、拒绝/取消计数，供慢客户端实验和面试故障分析使用。

---

## 10. 核心创新：分层 SLO 过载治理

### 10.1 创新定位

本项目不重写 TensorRT-LLM 内部连续批处理和 KV Cache 调度，而是在其上构建三层互补治理：

```text
Profile-guided SLO Policy
  └─ 软门禁：根据离线画像和当前负载估算违约风险，决定是否早拒绝
          ↓
2D Token Admission
  └─ 硬门禁：原子限制 active、input token 和 reserved output token
          ↓
Terminal-aware End-to-end Backpressure
  └─ 传输门禁：隔离慢消费者，保留 Cancel/Terminal，保证请求可收敛
          ↓
TensorRT-LLM Executor
  └─ 执行连续批处理、KV Cache 和 Decode 调度
```

创新点不是某一个复杂预测公式，而是把**性能风险、硬资源安全和端到端生命周期**分层组合，并用同一套请求状态与指标验证：过载时系统能否以可解释的早拒绝换取更高 SLO goodput，同时保持内存有界和资源最终归零。

该表述属于可验证的系统工程创新，不宣称论文级原创，也不把 TensorRT-LLM 上游调度能力描述为自研。

### 10.2 Profile-guided SLO Policy

离线 Profile 第一版只使用可稳定采集的维度：

| 维度 | v0.1 范围 |
|---|---|
| ISL bucket | 32、128 |
| OSL bucket | 32 |
| active request bucket | 1、2、4、8 |
| 负载快照 | active、input budget、reserved output budget、egress occupancy |
| 指标 | P50/P95 TTFT、TPOT、E2E、TPS |
| 元数据 | commit、Engine fingerprint、GPU、版本、随机种子 |

决策规则保持可解释：

```text
predicted_ttft_p95 + safety_margin <= request_ttft_slo
```

- 找不到匹配 bucket 时选择更保守邻近项或回退到硬 Admission，不进行激进外推；
- SLO Policy 只能拒绝，不能绕过 `AdmissionController` 接收请求；
- safety margin 第一版固定并通过验证集选择，不引入在线模型；
- Profile 只对固定模型、Engine 和硬件有效，manifest 不一致时自动禁用；
- 预测误差必须与收益一起报告。

### 10.3 为什么不只使用固定并发

LLM 请求的输入和输出长度差异会导致 Prefill 工作量、KV 占用和 Decode 时间差异。固定并发把长度差异压缩成一个数字，容易在长请求混入时过度接收；2D Token Budget 提供硬资源近似，SLO Policy 再处理“容量未超限但延迟可能违约”的软风险。

### 10.4 对照实验

| 策略 | 目的 |
|---|---|
| Fixed Concurrency | 招聘方容易理解的基础基线 |
| 2D Token Budget | 验证输入/输出 Token 预算相对请求数的价值 |
| Profile-guided SLO + 2D Token | 验证早拒绝对 SLO goodput 和无效 GPU 工作的影响 |

分层流控作为所有安全实验的共同底座，另设慢客户端和突发流量专项，验证：

- 单个慢客户端是否影响其他请求 TTFT/TPOT；
- 峰值缓冲事件和内存是否保持上限；
- Backpressure、Cancel 和 Terminal 是否全部收敛；
- 压力结束后 active、Token budget、Mailbox 和请求状态是否归零。

只有在固定实验中观察到稳定收益，简历才使用“提升 X%”或“降低 Y%”；否则如实记录无收益及原因，把结果作为工程边界分析。

---

## 11. Benchmark 与可观测性

### 11.1 分层 Benchmark

| 路径 | 目的 |
|---|---|
| Direct Backend | 建立 TensorRT-LLM 上层最小开销基线 |
| Worker IPC | 测量序列化、进程切换和会话流控开销 |
| HTTP/SSE | 测量完整用户路径和慢客户端影响 |

支持 closed-loop 正确性验证和 open-loop 过载实验。Open-loop 使用固定间隔或 Poisson 到达，至少覆盖 ISL32/OSL32、ISL128/OSL32 及二者混合 workload。

### 11.2 必须报告的指标

| 类别 | 指标 |
|---|---|
| 时延 | TTFT P50/P95、TPOT/ITL P50/P95、E2E P50/P95 |
| 吞吐 | request/s、output token/s |
| SLO | goodput、SLO attainment、accepted 后 timeout 数 |
| Admission | offered/accepted/rejected rate、拒绝原因 |
| 流控 | 各层队列高水位、backpressure/cancel 数、慢客户端影响 |
| 资源 | GPU 利用率、显存、active/scheduled/queued、KV used blocks |
| 正确性 | 成功、失败、重复 Terminal、Terminal 后 Delta、挂起请求 |
| 回收 | active、Token budget、Mailbox、请求映射和 KV 是否回到基线 |

实验必须固定模型、精度、Engine config、commit、workload、随机种子和 GPU。每个主要数据点独立运行至少 3 次；样本不足 1000 时不把 P99 作为主要结论。

### 11.3 指标所有权

- Gateway：HTTP 请求、SSE 断连、外部时延、IPC reconnect/epoch；
- Worker：Admission、Runtime 状态、IPC 队列、Terminal 原因；
- Backend：iteration stats、Engine active/queued、KV 使用；
- 所有层共享 request ID，日志不得记录完整 prompt。

---

## 12. 关键技术取舍总表

| 决策 | 采用方案 | 未采用方案 | 原因与代价 |
|---|---|---|---|
| 服务边界 | Python Gateway + C++ Worker | 单进程全 C++ 或全 Python | 保留生态效率并隔离 Legacy ABI；代价是 IPC 复杂度 |
| 本机通信 | Unix Domain Socket | TCP、gRPC、共享内存 | v0.1 易实现和调试；不支持跨机 |
| 序列化 | 长度前缀严格 JSON | Protobuf/FlatBuffers | 先保证协议正确；用数据决定是否二进制化 |
| Token 交付 | 批量 `TokenDelta` | 固定每 Token 一帧 | 降低 JSON 和系统调用开销；可能增加极小 flush 延迟 |
| 请求隔离 | 每请求有界队列 + 公平 writer | 单共享无界 FIFO | 防止慢请求队头阻塞；实现稍复杂 |
| 资源释放 | Mailbox Terminal 提交后释放 Lease | 客户端读取后释放 | GPU资源不受慢网络绑架；网络可靠性交给外层 |
| Admission | 软 SLO Policy + 硬 Token Budget | 单一预测器或固定并发 | 预测失误不能突破硬安全边界 |
| 模型管理 | 固定单模型 + Manifest | 动态多模型 | 便于一致性与实验复现；功能范围有限 |
| 可观测 | 分层指标 + request ID | 只采集总 TPS | 支持定位 Direct、IPC 和 HTTP 开销；采集本身需控制成本 |
| 失败恢复 | epoch + 合成 `Unavailable` | v0.1 自动重试生成 | 自动重试可能重复计算和破坏 exactly-once 语义 |

---

## 13. 本周执行顺序

| 时间 | Task | 主要交付物 | 阶段门禁 |
|---|---|---|---|
| Day 1 | UDS Transport / IpcSession | Client/Server、Hello、版本拒绝、半帧、断连、关闭 | Fake Socket 契约和真实 UDS 测试通过 |
| Day 2 | Worker 请求链路 | Submit、Accepted/Rejected、Cancel、TokenDelta、Terminal、请求归属 | Fake Backend 端到端通过，无重复/丢失终态 |
| Day 3 | 真实 Engine 与故障链路 | `llm_worker`、ModelManifest、真实 Token 回归、断连与stop | Direct 与 IPC Token 一致，资源归零 |
| Day 4 | 最小 Gateway | Tokenizer、非流式、SSE、health/ready、断连 cancel | 官方 OpenAI 客户端可调用，流式与非流式一致 |
| Day 5 | Open-loop 与分层流控实验 | 固定 workload、队列高水位、慢客户端和突发流量 | 内存有界，慢客户端不阻塞其他请求 |
| Day 6 | SLO Policy 与正式矩阵 | Profile JSON、三策略对照、3次重复 | 收益、误差、拒绝代价和边界可解释 |
| Day 7 | 求职交付与冻结 | CPU-only CMake/CI、README、架构与性能报告、tag | 文档与代码一致，不再增加功能 |

### 13.1 止损规则

1. Day 2 前 IPC 请求生命周期未闭环，不开始 Gateway。
2. Day 3 前真实 Engine IPC 路径未通过，SLO Policy 降级为设计与离线分析，不进入简历性能数字。
3. Day 4 前 SSE 未稳定，只保留非流式 API 和 IPC 流式 Demo，优先保证 Worker 架构完整。
4. 正式实验时间不足时，只保留两组可信 workload，不生成大量不可解释的数据。
5. SLO Policy 没有稳定收益时不得调参隐藏负结果；保留数据并解释适用边界。
6. 每完成一个阶段立即同步代码、测试和状态，禁止最后一天集中补文档。

### 13.2 A6 提交边界与阶段证据

“一步到位”表示 A6 最终数据面完整，不表示使用一个不可审查的巨型提交。实施固定拆为以下可独立回归的证据链：

| 提交 | 交付物 | 必须通过的门禁 |
|---|---|---|
| A6-1 Protocol | 补齐 Submit/Accepted/Rejected/Cancel/TokenDelta/Terminal/Health/Stats、ModelManifest 与严格 codec | 纯 CPU round-trip、非法字段、版本、长度和 enum 测试 |
| A6-2 Transport/Session | UDS Listener/Connection、半帧/断连、Session reader/writer、正常关闭 | 真实 UDS CPU 测试；FD/线程无残留；stop 有超时 |
| A6-3 Runtime Bridge | Request ownership、Accepted 顺序、EventForwarder、公平 egress、断连 cancel | Fake Backend 端到端；唯一 Terminal；资源与队列归零 |
| A6-4 Worker/Client | 独立 `llm_worker` 与 Python `GenerationClient` | 跨进程流式/非流式一致；Worker 断连合成 `Unavailable` |
| A6-5 Engine Evidence | 真实 Engine Token 回归和 Direct/IPC 对照 | Token IDs 完全一致；固定 workload 的 IPC 增量开销可解释 |

每个提交同时保存：对应测试命令、结果、关键状态机或故障语义变化，以及尚未覆盖的限制。禁止只保存最终截图而缺少可复现命令和 commit 关联。

---

## 14. 求职版 Definition of Done

### 14.1 架构与功能

- [x] `GenerationRuntime` 已接入 Backend、Admission 和 Mailbox。
- [x] IPC v1 握手、完整消息、严格校验和帧 codec 已完成 CPU 契约。
- [x] `UdsListener/UdsConnection/IpcSession` 完成真实 UDS、握手、有界写出、断连和可唤醒停止契约。
- [x] `RuntimeBridge/SessionEgress` 完成请求归属、Accepted 先行、EventForwarder、公平 Egress、Terminal 预留、请求级背压和断连取消契约。
- [x] `WorkerServer` 完成单活 Session 管理，并通过 Runtime Bridge 实现请求多路复用。
- [x] 独立 `llm_worker` 是唯一链接 Legacy TensorRT-LLM 的服务进程。
- [x] Gateway 完成固定模型 Tokenizer、非流式和 SSE 最小接口。
- [x] ModelManifest 在 `GenerationClient` / Worker 握手时校验；Gateway 接入后复用同一校验。
- [x] health、readiness 和最小 metrics 语义明确。

### 14.2 生命周期与可靠性

- [x] Runtime CPU 测试覆盖启动失败、提交异常、Admission 拒绝、终态归还、cancel、timeout、backpressure、stop 与竞态。
- [x] Transport CPU 测试覆盖真实 UDS、半帧、版本/模型拒绝、对端断连、有界 egress、阻塞 writer 停止和 FD 回收。
- [x] Accepted 与 Terminal 的跨进程语义通过真实 Worker/Engine 集成测试；Rejected 不创建活动句柄由 RuntimeBridge 与 Python `GenerationClient` 契约覆盖。
- [x] 非法帧、版本错误、Worker 断连和 graceful shutdown 已由 Protocol、Transport 与真实 Worker 集成测试覆盖。
- [x] Gateway 断连和慢消费者完成端到端覆盖；HTTP断连Cancel已验证，慢客户端三轮正式配对证据已落盘。
- [x] 各层队列、帧和请求体具有明确上限与高水位指标；慢客户端实验记录SSE buffer和Worker Session Egress高水位。
- [ ] 压力结束后请求状态、Admission预算、队列和KV资源回到基线。
- [x] Worker 侧验证 Accepted 先于同请求所有 Delta/Terminal、Rejected 后无 Terminal，并在正常完成、取消、背压和断连路径验证唯一终态。
- [ ] Gateway 侧验证 Accepted/Rejected/Terminal 顺序与唯一终态。
- [x] 第二个 Gateway Session 被明确拒绝；项目和简历不宣称尚未实现的多 Session 路由。
- [ ] Session reader/writer、RequestPump 数量均受配置硬限制，停止后线程和 FD 回到基线。

### 14.3 创新与性能

- [x] Direct 与 IPC 路径使用相同固定 workload，逐请求 Token IDs 完全一致。
- [x] HTTP 路径接入同一固定 workload，并与 Direct/IPC 完成三轮轮换分层对照；当前代码/证据提交为 `b192344` / `e376f78`，正式实验元数据保留历史重建前的绑定提交 `3f831fa`。
- [x] Open-loop Harness 支持 offered rate、goodput、reject reason 和 SLO 指标；commit `c37cc81` 已完成代码与 CPU 契约，真实过载数据仍由后续矩阵门禁负责。
- [x] 完成 Fixed Concurrency、2D Token Budget、Profile-guided SLO 三策略对照；16/24/32 RPS 各三轮并平衡执行顺序。
- [x] 完成慢客户端隔离和终态优先流控实验；三轮中健康请求339/339满足SLO、所有Terminal和资源收敛。
- [x] A6-5 Direct/IPC 主要数据点独立重复 3 次，并交错执行以降低时间漂移。
- [x] A6-5 报告 IPC 开销和适用边界，不把波动解释为性能收益。
- [x] 正式过载矩阵报告 SLO 收益、预测误差、拒绝代价和适用边界；当前 Profile-guided 为过度早拒绝的负结果。

### 14.4 求职交付

- [x] CPU-only configure/build/test 不依赖 TensorRT、CUDA、MPI 或真实 Engine。
- [ ] CPU 测试进入 CI。
- [x] GPU 测试由 Engine 路径门禁并明确标记为 `gpu;integration`。
- [ ] 提供 build、test、serve、benchmark 一键入口。
- [ ] README、架构文档、状态机、故障传播和性能报告与代码一致。
- [ ] 简历中的所有功能和性能数字均可由仓库证据复核。
- [ ] 准备 3～5 分钟项目介绍和 15 分钟深入讲解材料。

---

## 15. 简历证据目标

### 15.1 Serving / AI Infra 版本

> 基于 C++17 与 TensorRT-LLM Executor 构建单 GPU 流式 LLM Serving Runtime，设计独立 Worker/UDS 进程隔离、显式请求状态机、有界 Mailbox、唯一终态及 RAII Token Admission，覆盖取消、超时、背压、断连和优雅停止，并提供最小 OpenAI-compatible SSE Gateway。

### 15.2 系统创新版本

> 设计分层 SLO 过载治理，将 Profile-guided 软门禁、输入/预留输出 Token 硬 Admission 与终态优先的端到端有界流控结合；在固定混合长度 open-loop 负载下，对比固定并发与静态 Token Budget，量化 SLO goodput、P95 TTFT、拒绝代价、预测误差和慢客户端隔离效果。

正式实验已完成，但当前 Profile-guided 没有正向 goodput 收益；第二条不加入“提升 X%”，面试中改为报告 Token Budget 的延迟收益、Profile 高估约 `16.2 ms` 及过度早拒绝的边界。

### 15.3 求职表述边界

| 可以写入简历/面试 | 不得宣称 |
|---|---|
| 参考 vLLM/SGLang 的 API/Engine 多进程边界，独立设计版本化 UDS 数据面 | ChatGPT、Claude 或其他闭源系统内部同样使用本项目协议 |
| 自研 RequestState、Mailbox、Admission、IPC 生命周期和分层背压 | 自研 TensorRT-LLM 连续批处理、Paged KV Cache 或 GPU Kernel |
| 在固定模型、Engine、GPU 和 workload 下测得的 Direct/IPC/HTTP 数据 | 未重复验证、未关联 commit/Engine 的性能数字 |
| v0.1 单 Gateway、单 Worker、单模型的明确边界与故障收敛 | 多 Worker 路由、自动恢复、多 GPU 或生产集群能力 |

面试材料除架构图外，必须准备一张 Accepted/Rejected/Terminal 状态图、一张 Gateway 断连/Worker 断连故障传播图，以及一张 Direct/IPC/HTTP 分层数据表。它们比继续增加可选功能更能证明系统设计、并发正确性和性能分析能力。

面试讲解主线：

```text
为什么隔离 Legacy TensorRT-LLM Worker
→ 为什么 Accepted/Rejected 与 Terminal 必须区分
→ 如何保证唯一 Terminal 和 Lease 恰好释放一次
→ 为什么每一层都必须有容量边界
→ 如何隔离慢 SSE 客户端与 IPC 队头阻塞
→ 为什么请求数不足以刻画 LLM 负载
→ 为什么软 SLO Policy 不能替代硬 Admission
→ 如何通过 open-loop 对照实验评价收益和代价
```

---

## 16. 当前启动点

当前已完成；除特别标注外均已推送：

| 提交 | 内容 | 验证 |
|---|---|---|
| `82a750b` | `GenerationRuntime` 生命周期闭环 | Fake Backend CPU 契约覆盖 start/submit/Terminal/cancel/stop 与资源归还 |
| `91c79ae` | IPC v1 帧协议与握手契约 | 拆分/粘连帧、非法长度、JSON、版本、epoch 和链接隔离；6项CPU测试通过 |
| `9e5196d` | IPC v1 全量消息编解码与校验（A6-1） | 覆盖 Submit/Accepted/Rejected/Cancel/TokenDelta/Terminal/Health/Stats、ModelManifest、WorkerLimits、字段保真、稳定字符串、严格解码和编码校验；重新构建成功，6项CPU测试通过 |
| `b625df0` | 真实 UDS Transport 与 IpcSession 生命周期（A6-2） | 新增 `llm_ipc_transport_contract`；7项CPU回归通过；Transport 100次重复通过；ASan/UBSan 通过 |
| `050a263` | Runtime Bridge 与终态感知公平 Egress（A6-3） | Fake Backend + 真实 UDS 端到端覆盖 Submit 转换、Accepted 顺序、唯一 Terminal、幂等 Cancel、请求级背压、断连取消、Health/Stats 和资源归零；8项CPU回归通过 |
| `5b3922b` | 推理请求完整 Pipeline 文档 | `docs/Inference_Pipeline.md` 按请求进入顺序映射 IPC、Runtime、Admission、TensorRT-LLM Backend、Mailbox、Egress 与返回链路 |
| `392ae93` | 模块化源码与 CPU-only 构建边界 | OFF 模式从空目录构建且无 GPU/MPI 动态依赖，ON 模式完整构建；两条路径的 8 项 CPU 契约均通过 |
| `1676515` | 单活 `WorkerServer`、独立 `llm_worker` 与 Python `GenerationClient` | WorkerServer 与 Python Client 契约加入回归；最新 CPU-only 空目录构建 `10/10 PASS` |
| `a2f9c9b` | 真实 Worker / Engine 跨进程推理测试 | Direct Backend 基线 `9.40 s`、Python → UDS → Worker → TinyLlama Engine `8.21 s`；两条路径 Token IDs 完全一致，GPU 标签回归 `2/2 PASS` |
| `2589c4e` | 流式一致性与真实 Worker 断连验证（A6-4） | 真实 Engine 流式/非流式 Token IDs 均为 `(3, 29966, 29989, 5205, 29989)`；Worker `SIGKILL` 后两个已 Accepted 请求分别合成唯一 `Unavailable` Terminal；集成测试 `36.80 s` 通过 |
| `3334046` | Direct / IPC 增量开销量化（A6-5，已推送） | C1/ISL4/OSL32 固定 workload，每路径每轮200请求、3轮交错配对；Direct/IPC Token IDs 全量一致，失败、Rejected、Backpressure、Cancel 均为0且资源归零；TTFT P50 `+0.661 ms/+8.13%`，TPOT P50 `+0.025 ms/+0.33%`，E2E P50 `+1.482 ms/+0.61%`，吞吐 `-0.66%` |
| `0581c0a` | 固定单模型 OpenAI-compatible Gateway（已推送） | Gateway Core/API 契约加入 CPU 回归；真实 TinyLlama Worker 链路完成非流式、SSE 和 HTTP 断连 Cancel/Terminal 排空验证；本轮无 GPU `13/13 PASS`、真实 GPU `2/2 PASS` |
| `c37cc81` | HTTP/SSE closed/open-loop Benchmark Harness（已推送） | 新增有界 `max_inflight`、固定/Poisson 到达、SLO goodput、拒绝/终态/客户端失败分类、慢客户端分组、Gateway/Worker 高水位和资源归零检查；HTTP Benchmark 5 项、Gateway Core 10 项、Gateway API 3 项契约通过；当前构建无 GPU 回归 `14/14 PASS` |
| `c3f24ab` | 修正 Gateway 正常停止误判（已推送） | 区分 Harness 主动发送的 `SIGTERM` 与 Gateway 提前异常退出，避免正常收尾返回码 `-15` 阻断 summary/CSV 写入；新增两项停止契约，完整无 GPU 回归 `14/14 PASS`；真实 Engine C1 开发冒烟 5/5 请求成功并验证资源归零 |
| `b192344` | Direct/IPC/HTTP 三路径轮换总控 | 固定 Chat Template 生成同一 Token IDs，三阶轮换执行；逐请求校验 Direct/IPC Token IDs、HTTP 解码文本和 completion token 数一致，并汇总三层配对增量；空目录 CPU-only `15/15 PASS`、完整 GPU 构建 `17/17 PASS` |
| `e376f78` | C1/ISL17/OSL32 正式三路径基线证据 | 每路径每轮200请求、3轮轮换；600组输出一致且所有请求成功，HTTP 每轮资源归零；summary 与聚合 JSON 已进入 `benchmark/evidence/` |
| `2b08de1` | 慢客户端隔离正式证据 | 8 RPS open-loop、ISL17/OSL32、三轮control/slow交替配对；Control与Slow各360/360完成，健康请求339/339满足SLO，资源归零；原始summary与聚合JSON已进入`benchmark/evidence/` |
| `2e306ce` | SLO 软门禁与正式矩阵总控 | Profile 身份校验、可解释早拒绝、三策略轮换、预测误差和配对 goodput 统计完成；SLO Policy 契约纳入 CPU 回归 |
| `a51e0df` | ISL32/128、active 1/2/4/8 SLO 画像 | 8个数据点各3轮、每轮160请求；干净提交、Engine fingerprint 固定且所有资源归零 |
| `969897b` | 隔离矩阵预热负载 | 预热固定为 C1，不在测量前触发策略拒绝；该干净提交上完成 16/24/32 RPS 三策略×3轮正式矩阵 |

下一项唯一启动任务：

> **进入 Day 7 求职交付冻结：先完成 CPU CI 和 build/test/serve/benchmark 一键入口，再核对 README、架构/故障图、性能报告与简历数字，最后准备 3～5 分钟项目介绍和 15 分钟深入讲解材料。**

A6-1 已由提交 `9e5196d` 完成，A6-2 已由提交 `b625df0` 完成，A6-3 已由提交 `050a263` 完成；模块化源码与 CPU-only 构建边界由提交 `392ae93` 完成，Worker/Client 基础实现由提交 `1676515` 完成。2026-08-23 从空目录验证 OFF 模式 `10/10 PASS`（`0.29 s`）；提交 `a2f9c9b` 增加真实跨进程 GPU 门禁，Direct Backend 与 Python Client → UDS → `llm_worker` → TinyLlama Engine 两项测试分别在 `9.40 s` 和 `8.21 s` 内通过，输出 Token IDs 均为 `(3, 29966, 29989, 5205, 29989)`。提交 `2589c4e` 于 2026-08-24 完成 A6-4 门禁：真实 Engine 流式/非流式结果与 Direct 基线一致，两个已 Accepted 请求在 Worker `SIGKILL` 后分别收敛为唯一 `Unavailable` Terminal，集成测试在 `36.80 s` 内通过。提交 `3334046` 完成 A6-5 量化代码与正式运行并已推送至 `origin/main`：C1/ISL4/OSL32 固定 workload 下每路径每轮200请求、3轮交错配对，Token IDs 全量一致且测量期异常计数为0；IPC 相对 Direct 的 TTFT P50 增加 `0.661 ms`、E2E P50 增加 `1.482 ms`、吞吐下降 `0.66%`，说明进程/协议/Runtime 隔离成本主要体现为亚毫秒级首 Token 固定开销，稳态 Decode 基本不变。提交 `0581c0a` 完成最小 Gateway 并推送至 `origin/main`：固定模型 Tokenizer/Chat Template/Manifest 严格校验、OpenAI-compatible 非流式/SSE、health/readiness/metrics、有界请求与 SSE 输出以及断连 Cancel/后台排空均已实现；Gateway Core/API 与 Client 契约通过，2026-08-24 当前构建无 GPU 回归 `13/13 PASS`（`1.21 s`），真实 Direct 与 Worker/Engine GPU 回归 `2/2 PASS`（`36.69 s`）。真实 HTTP 验证中非流式与 SSE 均输出 `Certainly! Here are some examples`，usage 为 `17/8/25`；客户端主动断连后 `http_disconnect_cancels_total=1`、活动请求回到0，Gateway 与 Worker 均优雅停止。Direct/IPC/HTTP fixed-workload 已由当前提交 `b192344` 与 `e376f78` 完成（正式实验元数据仍绑定重建前的 `3f831fa`），下一项唯一任务切换为慢客户端隔离和正式过载矩阵，不提前声明尚未取得的 SLO 收益。

2026-08-25 提交 `c37cc81` 已推送至 `origin/main`：HTTP/SSE Benchmark 将纯 HTTP 客户端、负载调度和报告统计拆为 `http_benchmark_core.py`，入口负责隔离启动 Worker/Gateway、固定 Manifest/Tokenizer、保存 commit/Engine/GPU 元数据并拒绝覆盖既有证据；open-loop 的客户端任务数由 `max_inflight` 硬限制，过量负载显式记录为 `client_overflow`。Gateway metrics 新增 active/SSE buffer 高水位以及 Worker Admission、Session egress 当前值/高水位，抓取 metrics 不会为断开的 Worker 隐式重连。本次 CMake 已注册 14 项无 GPU 契约；首次完整回归中 `llm_worker_server_contract` 瞬时出现一次 `IPC session is closed`，随后该测试连续 `10/10 PASS`，再次完整回归 `14/14 PASS`（`1.69 s`），暂按偶发竞态观察项记录。两次真实 HTTP 开发运行只留下 Worker/Gateway 日志、未生成 summary/CSV，不能作为完成证据；下一步必须先保留 controller 错误并修正失败点，再进入正式分层实验。

2026-08-25 提交 `c3f24ab` 已推送至 `origin/main`：重新运行真实 Engine HTTP C1 冒烟后确认请求与推理链路已经完成，失败发生在 Harness 收尾阶段——Harness 主动向 Gateway 发送 `SIGTERM`，但 `ManagedGateway.stop()` 将预期返回码 `-15` 误判为异常，从而在 summary/CSV 写入前退出。修复仅在 Harness 确认由自身发起停止时接受 `-SIGTERM`，Gateway 提前以相同信号退出仍判为失败，并新增两项契约覆盖这一区分。修改后完整无 GPU 回归 `14/14 PASS`（`2.10 s`）；真实 TinyLlama C1 开发冒烟使用 prompt `Hello`、1 次预热、5 个测量请求和 OSL8，5/5 请求成功，输出均为 `Certainly! Here are some examples`，summary/CSV/Worker/Gateway 日志完整生成且 `resources_released=true`。该轮在提交前使用 `--allow-dirty`，只证明修复有效，不作为正式 HTTP 性能证据；下一步必须在干净提交 `c3f24ab` 上不带 `--allow-dirty` 重跑固定 workload。

2026-08-25 功能分支提交 `b192344` 新增 `run_direct_ipc_http_benchmark.py`：参考 NVIDIA TensorRT-LLM Triton Backend 的 core/end-to-end 分层测量思路，使用同一 Chat Template 和 Tokenizer 生成输入 Token IDs，按三阶顺序轮换 Direct、IPC、HTTP，要求 Direct/IPC 输出 Token IDs、HTTP 解码文本、completion token 数、成功请求和资源释放全部一致。新增契约后空目录 CPU-only 回归 `15/15 PASS`（`1.35 s`），完整 GPU 构建回归 `17/17 PASS`（`37.36 s`）。在历史重建前的同一代码树提交 `3f831fa` 上完成 C1/ISL17/OSL32 正式实验，每路径每轮200个测量请求、3轮轮换；IPC 相对 Direct 的 TTFT/TPOT/E2E P50 分别增加 `0.797/0.061/2.686 ms`，吞吐下降 `0.97%`，HTTP 相对 IPC 分别增加 `1.915/0.020/2.918 ms`，吞吐下降 `1.07%`。三路径所有请求成功，600组 Direct/IPC Token IDs 与 HTTP 解码文本逐请求一致，HTTP 每轮资源归零。当前提交 `e376f78` 保留聚合报告与每轮 summary 到 `benchmark/evidence/direct-ipc-http-c1-isl17-osl32-r3/`；下一步切换为慢客户端隔离，仍不提前声明 SLO Policy 收益。

2026-08-25 收工记录：PR #1 的非 Markdown 代码与正式证据保持不变，并按本地文档策略重建为 `b192344` / `e376f78`，`origin/main` 当前 merge 提交为 `3ba29e2`。相对基线 `c3f24ab`，本轮没有任何 `.md` 或 `.gitignore` 差异；根 `README.md` 和本进度文档仍保留在 lab-host 工作区并命中 `*.md` 忽略规则，等项目信息稳定后再单独加入 README。旧新代码树差异仅为 README 与忽略例外，因此未重复运行测试；当前功能分支工作区干净且与远端同步。下次从“慢 SSE 客户端隔离实验”继续，优先验证健康客户端 TTFT/E2E、各层高水位、Terminal 收敛与资源归零，再进入正式 open-loop 过载矩阵。

2026-08-26 在干净提交 `e376f78` 上完成慢客户端隔离正式实验：8 RPS constant-arrival open-loop、ISL17/OSL32、每组每轮120个请求，三轮按Control→Slow、Slow→Control、Control→Slow交替配对；slow组每16个请求设置1个慢消费者，每个非空SSE内容块延迟`62.5 ms`，慢请求E2E P50为`2022.943 ms`。Slow相对Control的健康TTFT P50/P95/P99配对均值增加`0.055/0.572/0.779 ms`，健康E2E P50/P95/P99变化`-0.256/+3.804/+3.398 ms`；Control和Slow各360/360完成，健康请求339/339满足TTFT `50 ms`与E2E `400 ms` SLO，无Rejected、Terminal Error、Client Failure、Backpressure Cancel或Terminal Drain Timeout，六组资源全部归零，输出文本哈希一致。closed-loop校准因慢请求占用负载发生器线程并形成同步突发而排除，不用于隔离结论；当前结论只覆盖非饱和慢消费者，下一步进入正式open-loop过载矩阵。

2026-08-27 核对并固化干净提交 `969897b` 上的正式三策略 open-loop 过载矩阵：TinyLlama 1.1B FP16 BS8 Engine、ISL32/128 各半、OSL32、16/24/32 RPS Poisson 到达，Fixed Concurrency、2D Token Budget 与 Profile-guided 各运行3轮，每轮240个测量请求，共27组、6480个请求。Fixed/Token/Profile 在16 RPS goodput为`13.644/10.027/5.760 RPS`，24 RPS为`12.072/12.425/6.355 RPS`，32 RPS为`12.129/13.638/6.404 RPS`。Token Budget 在24/32 RPS将 E2E P95 从 Fixed 的`425.444/426.366 ms`降至`304.864/314.173 ms`，但 goodput 配对差异方差较大，不声明稳定提升。Profile-guided 对 Accepted 请求保持零SLO miss，但画像相实际TTFT P95平均高估约`16.2 ms`，导致过度`slo_predicted_miss`早拒绝并明显降低goodput。所有Accepted均完成，无Terminal Error、Client Failure、Backpressure Cancel或Terminal Drain Timeout，可观测资源每轮归零。按止损规则保留负结果，不事后调参；下一步进入CPU CI、一键入口、文档与面试材料冻结。
